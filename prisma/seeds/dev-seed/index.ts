import type { SeedPlan } from "../seed-plan";

// Dev-only seed hub: keep local overrides and developer-specific fixture data here.
// Add new dev-only seed modules only when they are not part of the shared demo data.
export function registerDevSeedModules(_plan: SeedPlan) {}